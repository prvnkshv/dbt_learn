name: 'jaffle_shop'
version: '1.0.0'
profile: 'dbt_modelling'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  jaffle_shop:
    marts:
      +materialized: table
    staging:
      +materialized: view
